{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@sidebase/nuxt-auth": "^0.6.0-beta.2",
    "@types/node": "^20.3.1",
    "nuxt": "^3.5.2"
  },
  "dependencies": {
    "next-auth": "^4.22.1"
  }
}
